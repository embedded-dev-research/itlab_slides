# 04 — Target-based CMake

Highlights:
- PUBLIC/PRIVATE usage requirements.
- Compiler warnings as errors (portable generator expressions).

Build:

```
cmake -S . -B build
cmake --build build
```

